{
  "version": "1.0",
  "configurations": [    
      {
          "name": "Launch Local Server",
          "type": "node-terminal",
          "request": "launch",
          "cwd": "${workspaceFolder}",
          "command": ".\\.env\\Scripts\\activate&& mkdocs serve"
      },      
	  {
			"name": "Launch Local Server (no venv)",
			"type": "node-terminal",
			"request": "launch",
			"cwd": "${workspaceFolder}",
			"command": "mkdocs serve"
      }
  ]
}